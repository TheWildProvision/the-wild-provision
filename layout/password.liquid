<!doctype html>
<html lang="{{ request.locale.iso_code }}">
  <head>
    {% # Theme bundle built by Vite from src/. See layout/theme.liquid for details. %}
    {{ 'theme.css' | asset_url | stylesheet_tag }}
    <script src="{{ 'theme.js' | asset_url }}" type="module"></script>

    {% # Social, title, etc. %}
    {% render 'meta-tags' %}

    {{ content_for_header }}
  </head>

  <body>
    {{ content_for_layout }}
  </body>
</html>
